Installation

1. Install Python
2. Open console and navigate to script directory
3. Install packages using following command:
py -m pip install -r requirements.txt

Use

1. Create a "replays" folder inside the script directory
2. Launch script using following command:
py main.py
